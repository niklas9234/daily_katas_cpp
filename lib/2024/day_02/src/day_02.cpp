#include <iostream>
#include <algorithm>
#include <fstream>
#include <string>

void dai_02() {
    /*
    std::ifstream file("day_01.txt");
    std::string str;
    while (std::getline(file, str))
    {
        std::cout << str[0];
    }
    */
}